# ArkDle 服务器部署指南

本指南将帮助您在宝塔服务器上部署 `server.js` 文件，使其能够正常提供后端服务。

## 前提条件

1. 已安装宝塔面板的服务器
2. 已安装 Node.js 环境（推荐 v12 或更高版本）
3. 已完成域名绑定（可选）

## 部署步骤

### 步骤1：上传文件

1. 登录宝塔面板
2. 在文件管理器中，导航到您的网站根目录（通常类似于 `/www/wwwroot/您的域名`）
3. 确保 `js/server.js` 文件已上传到正确位置
4. 确保项目中其他相关文件（如 HTML、CSS、JS 等）也已上传

### 步骤2：创建 Node.js 项目

1. 在宝塔面板中，点击左侧菜单的【网站】
2. 点击【添加站点】，填写域名信息，选择创建目录并提交
3. 点击刚创建的站点，进入站点设置页面
4. 点击【Node.js】选项卡
5. 点击【版本管理】，安装合适版本的 Node.js（推荐 v12+）
6. 点击【项目管理】，然后点击【创建项目】
   - 项目名称：可以命名为 ArkDleServer
   - 项目路径：选择您网站根目录下的 `js` 文件夹
   - 启动文件：`server.js`
   - 端口：默认使用 3000（如果需要使用其他端口，请先在宝塔安全组中放行）
7. 点击【确定】完成创建

### 步骤3：配置环境变量（可选）

如果需要自定义服务器配置，可以在 Node.js 项目设置中添加环境变量：

1. PORT：服务器端口（默认 3000）
2. NODE_ENV：环境类型（development 或 production）

### 步骤4：设置反向代理（推荐）

为了让用户可以通过域名直接访问服务而不需要指定端口，建议设置反向代理：

1. 在站点设置页面，点击【反向代理】
2. 点击【添加反向代理】
   - 代理名称：ArkDleServer
   - 目标URL：`http://127.0.0.1:3000`
   - 发送域名：选择您的域名
3. 点击【保存】

### 步骤5：启动服务

1. 在 Node.js 项目管理页面，找到刚创建的项目
2. 点击【启动】按钮，等待服务启动成功
3. 启动成功后，可以通过【日志】按钮查看服务器运行状态

## 验证服务

服务启动后，可以通过以下方式验证：

1. 直接访问：`http://您的域名:3000/js/server`
2. 或通过反向代理：`http://您的域名/js/server`

如果服务正常，您将收到以下格式的 JSON 响应：

```json
{
  "message": "测试信息",
  "status": "success",
  "data": {
    "timestamp": "当前时间戳",
    "server": "ArkDle 测试服务器",
    "version": "1.0.0",
    "environment": "当前环境"
  }
}
```

## 常见问题处理

### 端口占用问题

如果启动失败并提示端口被占用：
1. 使用宝塔的【终端】执行 `netstat -tuln | grep 3000` 查看占用端口的进程
2. 可以选择终止占用端口的进程，或修改 `server.js` 中的端口配置
3. 也可以在 Node.js 项目设置中直接修改端口

### 访问权限问题

如果遇到 403 错误，可能是文件权限问题：
1. 在文件管理器中，选中 `js/server.js` 文件
2. 点击【权限】按钮，确保文件有执行权限
3. 建议设置权限为 755

### 跨域问题

`server.js` 已经配置了完整的 CORS 头部，如果仍遇到跨域问题：
1. 检查前端请求的 URL 是否正确
2. 确认服务器是否正确设置了 CORS 头部

## 日志管理

服务器会记录详细的访问日志和错误日志：
1. 在 Node.js 项目管理页面，点击【日志】查看服务运行日志
2. 也可以在宝塔的【网站】-【日志】中查看网站访问日志

## 更新维护

当需要更新代码时：
1. 先停止 Node.js 服务
2. 上传更新后的文件
3. 重新启动服务

## 注意事项

1. 在生产环境中，建议将 NODE_ENV 设置为 production
2. 定期备份项目文件和数据库（如果有）
3. 确保宝塔面板和服务器系统保持最新版本
4. 配置适当的安全组规则，只开放必要的端口
5. 考虑使用 PM2 等进程管理器来提高服务的稳定性（可选）

祝您部署顺利！如有其他问题，请查阅宝塔官方文档或寻求技术支持。